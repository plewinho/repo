<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
<div>
   <h1>
    {% if hour < 12%}
    Good morning, {{name|upper}}!
    <br>
    {{ "now"|date("m/d/Y") }}
    {% elseif hour < 18%}
    Good afternoon, {{name|lower}}!
    <br>
    {{ "now"|date("m/d/Y") }}
    {% else %}
    Good evening, {{name}}!
    {% endif %}
   </h1>



   {# map key value#}
   {% set people = {
    "Bob": "Smith",
    "Alice": "Dupond",
} %}

   
</div>
    
</body>
</html>